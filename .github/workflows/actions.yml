name: Actions for ci/cd lifecycle
on: [push]
jobs:
  jest:
    name: Jest testing
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/Checkout@v2
      - name: Build app
        run: npm run build
      - name: Run tests
        run: npm t
      
